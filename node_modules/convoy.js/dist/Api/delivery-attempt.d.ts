import { Client } from '../client';
export declare class DeliveryAttempt {
    private client;
    constructor(client: Client);
    all(eventDeliveryId: string, query?: any): Promise<any>;
    find(eventDeliveryId: string, deliveryAttemptId: string, query?: any): Promise<any>;
}
