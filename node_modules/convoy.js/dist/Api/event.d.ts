import { Client } from '../client';
import { CreateEvent, CreateFanOutEvent } from '../interfaces/event';
export declare class Event {
    private client;
    constructor(client: Client);
    all(query?: any): Promise<any>;
    create(attributes: CreateEvent, query?: any): Promise<any>;
    createFanOutEvent(attributes: CreateFanOutEvent, query?: any): Promise<any>;
    find(id: string, query?: any): Promise<any>;
}
