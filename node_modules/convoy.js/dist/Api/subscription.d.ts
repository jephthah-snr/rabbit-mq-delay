import { Client } from '../client';
import { CreateSubscription } from '../interfaces/subscription';
export declare class Subscription {
    private client;
    constructor(client: Client);
    all(query?: any): Promise<any>;
    create(attributes: CreateSubscription, query?: any): Promise<any>;
    find(id: string, query?: any): Promise<any>;
    delete(id: string, attributes?: any, query?: any): Promise<any>;
}
