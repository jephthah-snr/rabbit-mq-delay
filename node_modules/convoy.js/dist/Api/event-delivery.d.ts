import { Client } from '../client';
import { BatchResend } from '../interfaces/event-delivery';
export declare class EventDelivery {
    private client;
    constructor(client: Client);
    all(query?: any): Promise<any>;
    find(id: string, query?: any): Promise<any>;
    resend(id: string, query?: any): Promise<any>;
    batchResend(attributes: BatchResend, query?: any): Promise<any>;
}
