import { Client } from '../client';
import { CreateEvent, CreateFanOutEvent } from '../interfaces/event';
export declare class Kafka {
    private client;
    private producer?;
    private isConnected;
    constructor(client: Client);
    writeEvent(payload: CreateEvent | CreateFanOutEvent): Promise<import("kafkajs").RecordMetadata[] | undefined>;
}
